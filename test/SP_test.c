#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <libgeometry/Error.h>

#include <ctest.h>

CTEST(TEST_first,simple_firstelement)
{
    char* good = "circle(0 0, 10)";
    double resultgood = coordinatesx(good);
    ASSERT_DBL_NEAR_TOL(0, resultgood);
}

CTEST(TEST_first,simple_firstelement)
{
    char* good = "circle(0 0, 10)";
    double resultgood = coordinatesx(good);
    ASSERT_DBL_NEAR_TOL(0, resultgood);
}

CTEST(TEST_first,simple_firstelement)
{
    char* good = "circle(0 0, 10)";
    double resultgood = coordinatesx(good);
    ASSERT_DBL_NEAR_TOL(0, resultgood);
}

CTEST(TEST_first,simple_firstelement)
{
    char* good = "circle(0 0, 10)";
    double resultgood = coordinatesx(good);
    ASSERT_DBL_NEAR_TOL(0, resultgood);
}

CTEST(TEST_first,simple_firstelement)
{
    char* good = "circle(0 0, 10)";
    double resultgood = coordinatesx(good);
    ASSERT_DBL_NEAR_TOL(0, resultgood);
}

CTEST(TEST_first,simple_firstelement)
{
    char* good = "circle(0 0, 10)";
    double resultgood = coordinatesx(good);
    ASSERT_DBL_NEAR_TOL(0, resultgood);
}
