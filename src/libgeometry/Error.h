#include <stdio.h>
#include <stdlib.h>

int fileerror(FILE* file);
int figure(char* str);
int first(char* str);
int arg(char* str);
int num(char* str);
int flout(char* str);
int sumbol(char* str);
int end(char* str);
